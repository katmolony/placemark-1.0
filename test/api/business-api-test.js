import { assert } from "chai";
import { assertSubset } from "../test-utils.js";
import { placemarkService } from "./placemark-service.js";
import { maggie, greece, testLocations, testBusinesss, mcdonalds } from "../fixtures.js";

// import { maggie, greece, testPlaylists, testTracks, concerto } from "../fixtures.js";



suite("Business API tests", () => {
  let user = null;
  let locationList = null; //could be wrong beethovenSonatas

  setup(async () => {
  });

  teardown(async () => {});

  test("create business", async () => {
  });

  test("create Multiple businesss", async () => {
  });

  test("Delete Business", async () => {
  });

  test("test denormalised location", async () => {
  });
});